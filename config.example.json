{
  "port": 3000,
  "upstreams": [
    {
      "_comment": "stdio upstream example — runs a local MCP server as a child process",
      "name": "home-assistant",
      "transport": "stdio",
      "command": "npx",
      "args": ["-y", "@jango-blockchained/homeassistant-mcp"],
      "env": {
        "HASS_HOST": "${HASS_HOST}",
        "HASS_TOKEN": "${HASS_TOKEN}"
      },
      "namePrefix": "hass__",
      "allowAll": true
    },
    {
      "_comment": "streamable-http upstream example — connects to a remote MCP server over HTTP",
      "name": "github",
      "transport": "streamable-http",
      "url": "https://api.githubcopilot.com/mcp/",
      "headers": {
        "Authorization": "Bearer ${GITHUB_PERSONAL_ACCESS_TOKEN}"
      },
      "namePrefix": "github__",
      "allowAll": true
    },
    {
      "_comment": "sse upstream example — connects to a remote MCP server over Server-Sent Events",
      "name": "my-sse-server",
      "transport": "sse",
      "url": "https://my-mcp-server.example.com/sse",
      "headers": {
        "Authorization": "Bearer ${MY_SSE_TOKEN}"
      },
      "namePrefix": "myserver__",
      "allowAll": false,
      "allowedTools": ["tool_one", "tool_two"]
    }
  ]
}
